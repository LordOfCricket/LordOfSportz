{
  "name": "@karate/config",
  "version": "0.1.0",
  "private": true,
  "description": "Typed, validated environment configuration loader. Fails fast at startup on missing/invalid config rather than surfacing undefined at runtime.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "typescript": "^5.6.2"
  }
}
