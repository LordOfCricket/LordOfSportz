{
  "name": "@karate/constants",
  "version": "0.1.0",
  "private": true,
  "description": "Shared, non-configurable constants (nothing here should differ between environments).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
