{
  "name": "@karate/validation",
  "version": "0.1.0",
  "private": true,
  "description": "Shared zod validation schemas for DTOs used by web, mobile, and API — one source of truth for request/response shapes.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@karate/types": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
