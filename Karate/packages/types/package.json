{
  "name": "@karate/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript contracts (enums, DTOs, API response shapes) used by web, mobile, and API.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
