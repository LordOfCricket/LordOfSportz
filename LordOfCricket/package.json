{
  "name": "lord-of-cricket",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "dev:all": "concurrently -n client,server,mobile -c blue,green,cyan \"npm run dev --prefix client\" \"npm run dev --prefix server\" \"npm start --prefix mobile\"",
    "mobile": "npm start --prefix mobile",
    "mobile:android": "npm run android --prefix mobile",
    "mobile:ios": "npm run ios --prefix mobile"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
